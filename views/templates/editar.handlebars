<div class="container">
    <div class="col-md-6">
      <div class="container-form">
        <h2>Simule agora</h2>
        <p>Realize a sua alteração e comece a investir em uma experiência fácil e
          intuitiva:</p>
        <form id="form-simular" action="/simulacao/editar" method="POST">
          <div class="form-floating mb-3">
            <input type="hidden" name="id" value={{simulacao.id}}>
            <input
              type="text"
              name="titulo"
              class="form-control"
              id="titulo"
              placeholder="Simulação"
              value="{{simulacao.titulo}}"
            />
          </div>
          <div class="form-floating mb-3">
            <h4>O que você deseja conquistar com o seu investimento?</h4>
            <input
              type="text"
              name="objetivo"
              class="form-control"
              id="objetivo"
              placeholder="Objetivo"
              value="{{simulacao.objetivo}}"
            />
          </div>
          <div class="form-floating mb-3">
            <h4>Valor Inicial do seu investimento</h4>
            <input
              class="form-control"
              type="number"
              name="valor_investimento_inicial"
              id="valor_investimento"
              placeholder="R$ 0,00"
              value="{{simulacao.valor_investimento_inicial}}"
            />
          </div>
          <div class="form-floating mb-3">
            <h4>Valor Mensal a ser investido</h4>
            <input
              class="form-control"
              type="number"
              name="valor_investimento_mensal"
              id="valor_investimento_mensal"
              placeholder="R$ 0,00"
              value="{{simulacao.valor_investimento_mensal}}"
            />
          </div>
          <div class="form-floating mb-3">
            <h4>Quanto tempo pretende deixar seu dinheiro investido?</h4>
            <input
              type="number"
              name="tempo_de_investimento_meses"
              class="form-control"
              id="tempo_de_investimento_meses"
              placeholder="Meses"
              value="{{simulacao.tempo_de_investimento_meses}}"
            />
          </div>
          <button id="btn" class="btn btn-dark" type="submit">Salvar e Simular</button>
        </form>
      </div>
    </div>
    <div id="resultado" style="display: none;"  class="col-md-6">
        {{body}}
    </div>
  </div>
</div>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
    $(document).ready(function() {
        $("#form-simular").submit(function(e) {
            e.preventDefault();
            $.ajax({
                url: '/simulacao/editar', 
                method: 'POST',
                data: $(this).serialize(),
                success: function(data) {
                    $("#resultado").html(data);
                    $("#resultado").css("display", "block");
                }
            });
        });
    });
</script>
  